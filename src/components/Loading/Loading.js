import React from 'react';
import gif from '../../assets/images/loading.gif';

const Loading = () => {
    return <img src={gif} alt='loading' style={{ height: '120px' }}/>
};

export default Loading;
